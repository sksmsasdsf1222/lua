local Players = game:GetService("Players")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local TweenService = game:GetService("TweenService")

-- 곡선 모서리 적용 함수 (곡선 강도를 조정)
local function setRoundedCorners(frame, radius)
    frame.ClipsDescendants = true
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, radius) -- 모서리 곡선 강도를 조정합니다.
    corner.Parent = frame
end

-- X 버튼 생성 함수
local function createCloseButton(parent)
    local closeButton = Instance.new("TextButton")
    closeButton.Size = UDim2.new(0, 30, 0, 30)
    closeButton.Position = UDim2.new(1, -40, 0, 10)
    closeButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    closeButton.BorderSizePixel = 0
    closeButton.Text = "✕"
    closeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    closeButton.Font = Enum.Font.GothamBold
    closeButton.TextSize = 18
    closeButton.TextStrokeTransparency = 0.8
    closeButton.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    setRoundedCorners(closeButton, 10) -- 모서리 곡선 강도를 조정합니다.
    closeButton.Parent = parent

    closeButton.MouseButton1Click:Connect(function()
        parent:TweenSizeAndPosition(
            UDim2.new(0, 0, 0, 0),
            UDim2.new(0.5, 0, 0.5, 0),
            Enum.EasingDirection.In,
            Enum.EasingStyle.Sine,
            0.5,
            true,
            function() parent:Destroy() end
        )
    end)
    
    return closeButton
end

-- 업데이트 내용 버튼 생성 함수
local function createUpdateButton(parent)
    local updateButton = Instance.new("TextButton")
    updateButton.Size = UDim2.new(0, 120, 0, 40)
    updateButton.Position = UDim2.new(0.5, -60, 0, 10) -- 중앙에 위치
    updateButton.AnchorPoint = Vector2.new(0.5, 0)
    updateButton.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
    updateButton.BorderSizePixel = 0
    updateButton.Text = "업데이트 내용"
    updateButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    updateButton.Font = Enum.Font.GothamBold
    updateButton.TextSize = 18
    updateButton.TextStrokeTransparency = 0.8
    updateButton.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    setRoundedCorners(updateButton, 10) -- 모서리 곡선 강도를 조정합니다.
    updateButton.Parent = parent

    return updateButton
end

-- 중요 시스템 GUI 생성 함수
local function createImportantSystemGui()
    local systemGui = Instance.new("ScreenGui")
    systemGui.Name = "ImportantSystemGui"
    systemGui.Parent = playerGui

    local systemFrame = Instance.new("Frame")
    systemFrame.Size = UDim2.new(0.5, 0, 0.5, 0)
    systemFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
    systemFrame.AnchorPoint = Vector2.new(0.5, 0.5)
    systemFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30) -- 내용쪽 배경색 30, 30, 30
    systemFrame.BorderSizePixel = 0
    setRoundedCorners(systemFrame, 20) -- 모서리 곡선 강도를 조정합니다.
    systemFrame.Parent = systemGui

    createCloseButton(systemFrame)

    -- 업데이트 내용 버튼
    local updateButton = createUpdateButton(systemFrame)

    -- 제목 프레임
    local titleFrame = Instance.new("Frame")
    titleFrame.Size = UDim2.new(1, 0, 0, 50)
    titleFrame.BackgroundColor3 = Color3.fromRGB(65, 65, 65) -- 제목쪽 배경색 65, 65, 65
    titleFrame.BorderSizePixel = 0
    setRoundedCorners(titleFrame, 20) -- 모서리 곡선 강도를 조정합니다.
    titleFrame.Parent = systemFrame

    local titleLabel = Instance.new("TextLabel")
    titleLabel.Size = UDim2.new(1, 0, 1, 0)
    titleLabel.BackgroundTransparency = 1
    titleLabel.Text = "[ KR ] PRO ViSNT HUB"
    titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255) -- 제목 글씨 색상 흰색
    titleLabel.Font = Enum.Font.GothamBold
    titleLabel.TextSize = 24
    titleLabel.TextStrokeTransparency = 0.8
    titleLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    titleLabel.Parent = titleFrame

    -- 스크롤 가능한 프레임
    local scrollFrame = Instance.new("ScrollingFrame")
    scrollFrame.Size = UDim2.new(1, -20, 0.8, -60)
    scrollFrame.Position = UDim2.new(0, 10, 0, 60)
    scrollFrame.BackgroundColor3 = Color3.fromRGB(138, 138, 138) -- 배경색 8A8A8A
    scrollFrame.BorderSizePixel = 0
    scrollFrame.CanvasSize = UDim2.new(0, 0, 1, 0)
    scrollFrame.ScrollBarThickness = 12
    scrollFrame.Parent = systemFrame

    -- 스크롤 영역 안의 콘텐츠
    local uiListLayout = Instance.new("UIListLayout")
    uiListLayout.Parent = scrollFrame
    uiListLayout.Padding = UDim.new(0, 10)

    local basicSection = Instance.new("Frame")
    basicSection.Size = UDim2.new(1, -20, 0, 100)
    basicSection.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    basicSection.BorderSizePixel = 0
    setRoundedCorners(basicSection, 10)
    basicSection.Parent = scrollFrame

    local basicLabel = Instance.new("TextLabel")
    basicLabel.Size = UDim2.new(1, 0, 1, 0)
    basicLabel.BackgroundTransparency = 1
    basicLabel.Text = "기본 기능"
    basicLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    basicLabel.Font = Enum.Font.GothamBold
    basicLabel.TextSize = 20
    basicLabel.TextStrokeTransparency = 0.8
    basicLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    basicLabel.Parent = basicSection

    local advancedSection = Instance.new("Frame")
    advancedSection.Size = UDim2.new(1, -20, 0, 100)
    advancedSection.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    advancedSection.BorderSizePixel = 0
    setRoundedCorners(advancedSection, 10)
    advancedSection.Parent = scrollFrame

    local advancedLabel = Instance.new("TextLabel")
    advancedLabel.Size = UDim2.new(1, 0, 1, 0)
    advancedLabel.BackgroundTransparency = 1
    advancedLabel.Text = "고급 기능"
    advancedLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    advancedLabel.Font = Enum.Font.GothamBold
    advancedLabel.TextSize = 20
    advancedLabel.TextStrokeTransparency = 0.8
    advancedLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    advancedLabel.Parent = advancedSection
end

-- 비밀번호 입력과 알림 창 설정
local function setup()
    local passwordScreen, passwordBox, submitButton, mainFrame = createPasswordScreen()

    -- 비밀번호가 맞는지 확인할 예제 함수 (사용자의 비밀번호 입력 확인)
    local correctPassword = "1234"  -- 예제 비밀번호
    submitButton.MouseButton1Click:Connect(function()
        if passwordBox.Text == correctPassword then
            passwordScreen:Destroy()
            createImportantSystemGui()
        else
            createErrorNotification(mainFrame)
        end
    end)
end

setup()