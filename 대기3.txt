-- 로블록스 서비스 가져오기
local Players = game:GetService("Players")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local UIS = game:GetService("UserInputService")

-- 곡선 모서리를 지원하는 함수
local function setRoundedCorners(frame, radius)
    frame.ClipsDescendants = true
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, radius)
    corner.Parent = frame
end

-- X 버튼 생성 함수
local function createCloseButton(parent)
    local closeButton = Instance.new("TextButton")
    closeButton.Size = UDim2.new(0, 40, 0, 40)
    closeButton.Position = UDim2.new(1, -50, 0, 10)
    closeButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    closeButton.BorderSizePixel = 0
    closeButton.Text = "X"
    closeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    closeButton.Font = Enum.Font.GothamBold
    closeButton.TextSize = 24
    closeButton.TextStrokeTransparency = 0.8
    closeButton.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    setRoundedCorners(closeButton, 20)  -- 곡선 모서리 설정
    closeButton.Parent = parent

    closeButton.MouseButton1Click:Connect(function()
        parent:Destroy()
    end)
    
    return closeButton
end

-- 비밀번호 입력창 생성
local function createPasswordScreen()
    local passwordScreen = Instance.new("ScreenGui")
    passwordScreen.Name = "PasswordScreen"
    passwordScreen.Parent = playerGui

    -- 창 크기: 35%로 축소
    local mainFrame = Instance.new("Frame")
    mainFrame.Size = UDim2.new(0.35, 0, 0.35, 0)
    mainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
    mainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
    mainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    mainFrame.BorderSizePixel = 0
    setRoundedCorners(mainFrame, 20)  -- 곡선 모서리 설정
    mainFrame.Parent = passwordScreen

    createCloseButton(mainFrame)  -- X 버튼 추가

    local titleLabel = Instance.new("TextLabel")
    titleLabel.Size = UDim2.new(1, 0, 0, 50)
    titleLabel.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    titleLabel.Text = "[ KR ] Verification"
    titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    titleLabel.Font = Enum.Font.GothamBold
    titleLabel.TextSize = 26
    titleLabel.TextStrokeTransparency = 0.8
    titleLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    titleLabel.Parent = mainFrame

    local passwordBox = Instance.new("TextBox")
    passwordBox.Size = UDim2.new(0.8, 0, 0, 40)
    passwordBox.Position = UDim2.new(0.1, 0, 0.3, 0)
    passwordBox.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    passwordBox.BorderSizePixel = 0
    passwordBox.TextColor3 = Color3.fromRGB(255, 255, 255)
    passwordBox.Font = Enum.Font.Gotham
    passwordBox.TextSize = 20
    passwordBox.Text = ""
    passwordBox.ClearTextOnFocus = false
    passwordBox.TextTransparency = 0.5
    setRoundedCorners(passwordBox, 15)  -- 곡선 모서리 설정
    passwordBox.Parent = mainFrame

    local submitButton = Instance.new("TextButton")
    submitButton.Size = UDim2.new(0.8, 0, 0, 50)
    submitButton.Position = UDim2.new(0.1, 0, 0.6, 0)
    submitButton.BackgroundColor3 = Color3.fromRGB(50, 150, 50)
    submitButton.BorderSizePixel = 0
    submitButton.Text = "완료"
    submitButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    submitButton.Font = Enum.Font.GothamBold
    submitButton.TextSize = 22
    submitButton.TextStrokeTransparency = 0.8
    submitButton.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    setRoundedCorners(submitButton, 15)  -- 곡선 모서리 설정
    submitButton.Parent = mainFrame

    return passwordScreen, passwordBox, submitButton, titleLabel
end

-- 버튼 클릭 시 표시되는 다양한 화면 생성 함수
local function createButtonScreen(buttonText, contentText)
    local buttonScreen = Instance.new("ScreenGui")
    buttonScreen.Name = buttonText .. "Screen"
    buttonScreen.Parent = playerGui

    local mainFrame = Instance.new("Frame")
    mainFrame.Size = UDim2.new(0.8, 0, 0.5, 0)
    mainFrame.Position = UDim2.new(0.1, 0, 0.25, 0)
    mainFrame.BackgroundColor3 = Color3.fromRGB(220, 220, 220)
    mainFrame.BorderSizePixel = 0
    setRoundedCorners(mainFrame, 20)  -- 곡선 모서리 설정
    mainFrame.Parent = buttonScreen

    createCloseButton(mainFrame)  -- X 버튼 추가

    local contentLabel = Instance.new("TextLabel")
    contentLabel.Size = UDim2.new(1, 0, 1, 0)
    contentLabel.BackgroundTransparency = 1
    contentLabel.Text = contentText
    contentLabel.TextColor3 = Color3.fromRGB(0, 0, 0)
    contentLabel.Font = Enum.Font.GothamBold
    contentLabel.TextSize = 24
    contentLabel.TextStrokeTransparency = 0.8
    contentLabel.TextStrokeColor3 = Color3.fromRGB(255, 255, 255)
    contentLabel.Parent = mainFrame

    return buttonScreen
end

-- "안녕" 화면 생성 함수
local function createSuccessScreen()
    local successScreen = Instance.new("ScreenGui")
    successScreen.Name = "SuccessScreen"
    successScreen.Parent = playerGui

    -- 창 크기: 65%로 확대
    local mainFrame = Instance.new("Frame")
    mainFrame.Size = UDim2.new(0.65, 0, 0.65, 0)
    mainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
    mainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
    mainFrame.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
    mainFrame.BorderSizePixel = 0
    setRoundedCorners(mainFrame, 20)  -- 곡선 모서리 설정
    mainFrame.Parent = successScreen

    createCloseButton(mainFrame)  -- X 버튼 추가

    local titleLabel = Instance.new("TextLabel")
    titleLabel.Size = UDim2.new(1, 0, 0, 50)
    titleLabel.BackgroundColor3 = Color3.fromRGB(150, 150, 150)
    titleLabel.Text = "안녕"
    titleLabel.TextColor3 = Color3.fromRGB(0, 0, 0)
    titleLabel.Font = Enum.Font.GothamBold
    titleLabel.TextSize = 40
    titleLabel.TextStrokeTransparency = 0.8
    titleLabel.TextStrokeColor3 = Color3.fromRGB(255, 255, 255)
    titleLabel.Parent = mainFrame

    -- 버튼 패널 생성
    local buttonPanel = Instance.new("Frame")
    buttonPanel.Size = UDim2.new(0.3, 0, 0.8, 0)
    buttonPanel.Position = UDim2.new(0, 10, 0.1, 0)
    buttonPanel.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
    buttonPanel.BorderSizePixel = 0
    setRoundedCorners(buttonPanel, 15)  -- 곡선 모서리 설정
    buttonPanel.Parent = mainFrame

    -- 스크롤 추가
    local scrollingFrame = Instance.new("ScrollingFrame")
    scrollingFrame.Size = UDim2.new(1, -20, 1, -60)
    scrollingFrame.Position = UDim2.new(0, 10, 0, 50)
    scrollingFrame.BackgroundTransparency = 1
    scrollingFrame.ScrollBarThickness = 12
    scrollingFrame.Parent = buttonPanel

    local buttonListLayout = Instance.new("UIListLayout")
    buttonListLayout.Padding = UDim.new(0, 10)
    buttonListLayout.Parent = scrollingFrame

    -- 버튼 생성 함수
    local function createButton(buttonText, contentText)
        local button = Instance.new("TextButton")
        button.Size = UDim2.new(1, 0, 0, 60)
        button.BackgroundColor3 = Color3.fromRGB(120, 120, 120)
        button.BorderSizePixel = 0
        button.Text = buttonText
        button.TextColor3 = Color3.fromRGB(255, 255, 255)
        button.Font = Enum.Font.GothamBold
        button.TextSize = 20
        button.TextStrokeTransparency = 0.8
        button.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
        setRoundedCorners(button, 15)  -- 곡선 모서리 설정
        button.Parent = scrollingFrame

        button.MouseButton1Click:Connect(function()
            -- 클릭 시 새 창 생성
            local existingScreen = playerGui:FindFirstChild(buttonText .. "Screen")
            if existingScreen then
                existingScreen:Destroy()
            end
            createButtonScreen(buttonText, contentText)
        end)
    end

    -- 버튼 추가
    createButton("기본기능", "기본기능에 대한 내용입니다.")
    createButton("테러 기능", "테러 기능에 대한 내용입니다.")
    createButton("아침밥 기능", "아침밥 기능에 대한 내용입니다.")
    createButton("라이프 기능", "라이프 기능에 대한 내용입니다.")
end

-- 비밀번호 확인 및 GUI 전환
local function onPasswordSubmit(passwordBox, passwordScreen, titleLabel)
    local correctPasswordUser = "userpass"
    local correctPasswordAdmin = "adminpass"

    local password = passwordBox.Text

    if password == correctPasswordUser or password == correctPasswordAdmin then
        passwordScreen:Destroy()  -- 비밀번호 입력 창 제거
        createSuccessScreen()  -- 성공 화면 생성
    else
        -- 비밀번호가 틀린 경우
        passwordBox.Text = ""
        titleLabel.Text = "[ KR ] Verification"
        local errorMessage = Instance.new("TextLabel")
        errorMessage.Size = UDim2.new(1, 0, 0, 50)
        errorMessage.Position = UDim2.new(0, 0, 0.7, 0)
        errorMessage.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
        errorMessage.Text = "비밀번호를 틀렸습니다."
        errorMessage.TextColor3 = Color3.fromRGB(255, 255, 255)
        errorMessage.Font = Enum.Font.GothamBold
        errorMessage.TextSize = 20
        errorMessage.TextStrokeTransparency = 0.8
        errorMessage.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
        setRoundedCorners(errorMessage, 15)  -- 곡선 모서리 설정
        errorMessage.Parent = passwordScreen
    end
end

-- 메인 실행 로직
local passwordScreen, passwordBox, submitButton, titleLabel = createPasswordScreen()
submitButton.MouseButton1Click:Connect(function()
    onPasswordSubmit(passwordBox, passwordScreen, titleLabel)
end)