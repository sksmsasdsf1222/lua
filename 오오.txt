-- 로블록스 서비스 가져오기
local Players = game:GetService("Players")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")
local TweenService = game:GetService("TweenService")

-- 곡선 모서리 적용 함수 (곡선 강도를 조정)
local function setRoundedCorners(frame, radius)
    frame.ClipsDescendants = true
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, radius) -- 모서리 곡선 강도를 조정합니다.
    corner.Parent = frame
end

-- X 버튼 생성 함수
local function createCloseButton(parent)
    local closeButton = Instance.new("TextButton")
    closeButton.Size = UDim2.new(0, 30, 0, 30)
    closeButton.Position = UDim2.new(1, -40, 0, 10)
    closeButton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    closeButton.BorderSizePixel = 0
    closeButton.Text = "✕"
    closeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    closeButton.Font = Enum.Font.GothamBold
    closeButton.TextSize = 18
    closeButton.TextStrokeTransparency = 0.8
    closeButton.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    setRoundedCorners(closeButton, 10) -- 모서리 곡선 강도를 조정합니다.
    closeButton.Parent = parent

    closeButton.MouseButton1Click:Connect(function()
        parent:TweenSizeAndPosition(
            UDim2.new(0, 0, 0, 0),
            UDim2.new(0.5, 0, 0.5, 0),
            Enum.EasingDirection.In,
            Enum.EasingStyle.Sine,
            0.5,
            true,
            function() parent:Destroy() end
        )
    end)
    
    return closeButton
end

-- 비밀번호 입력창 생성
local function createPasswordScreen()
    local passwordScreen = Instance.new("ScreenGui")
    passwordScreen.Name = "PasswordScreen"
    passwordScreen.Parent = playerGui

    local mainFrame = Instance.new("Frame")
    mainFrame.Size = UDim2.new(0, 400, 0, 300)  -- 사이즈 변경
    mainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
    mainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
    mainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)  -- 배경색 변경
    mainFrame.BorderSizePixel = 0
    setRoundedCorners(mainFrame, 15)  -- 모서리 곡선 강도 변경
    mainFrame.Parent = passwordScreen

    -- 상단바 추가
    local topBar = Instance.new("Frame")
    topBar.Size = UDim2.new(1, 0, 0, 30)
    topBar.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    topBar.BorderSizePixel = 0
    setRoundedCorners(topBar, 15)
    topBar.Parent = mainFrame

    local titleLabel = Instance.new("TextLabel")
    titleLabel.Size = UDim2.new(1, 0, 1, 0)
    titleLabel.BackgroundTransparency = 1
    titleLabel.Text = "[ VIP ] BIOM SCRIPT HUB"
    titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    titleLabel.Font = Enum.Font.GothamBold
    titleLabel.TextSize = 18
    titleLabel.TextStrokeTransparency = 0.8
    titleLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    titleLabel.Parent = topBar

    createCloseButton(mainFrame)

    local buttonFrame = Instance.new("Frame")
    buttonFrame.Size = UDim2.new(1, 0, 1, -30)  -- 크기 조정
    buttonFrame.Position = UDim2.new(0, 0, 0, 30)
    buttonFrame.BackgroundTransparency = 1
    buttonFrame.Parent = mainFrame

    local passwordBox = Instance.new("TextBox")
    passwordBox.Size = UDim2.new(0.8, 0, 0, 50) -- 크기 조정
    passwordBox.Position = UDim2.new(0.5, 0, 0.2, 0)  -- 위치 조정
    passwordBox.AnchorPoint = Vector2.new(0.5, 0.5)
    passwordBox.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
    passwordBox.BorderSizePixel = 0
    passwordBox.TextColor3 = Color3.fromRGB(255, 255, 255)
    passwordBox.Font = Enum.Font.Gotham
    passwordBox.TextSize = 18
    passwordBox.Text = ""
    passwordBox.ClearTextOnFocus = false
    passwordBox.TextTransparency = 0.5
    setRoundedCorners(passwordBox, 10) -- 모서리 곡선 강도를 조정합니다.
    passwordBox.Parent = buttonFrame

    local submitButton = Instance.new("TextButton")
    submitButton.Size = UDim2.new(0.8, 0, 0, 45) -- 크기 조정
    submitButton.Position = UDim2.new(0.5, 0, 0.5, 0)  -- 위치 조정
    submitButton.AnchorPoint = Vector2.new(0.5, 0.5)
    submitButton.BackgroundColor3 = Color3.fromRGB(65, 65, 65) -- 버튼 배경색
    submitButton.BorderSizePixel = 2
    submitButton.BorderColor3 = Color3.fromRGB(97, 97, 97) -- 테두리 색상
    submitButton.Text = "로그인"
    submitButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    submitButton.Font = Enum.Font.GothamBold
    submitButton.TextSize = 20
    submitButton.TextStrokeTransparency = 0.8
    submitButton.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    setRoundedCorners(submitButton, 10) -- 모서리 곡선 강도를 조정합니다.
    submitButton.Parent = buttonFrame

    return passwordScreen, passwordBox, submitButton, mainFrame
end

-- 비밀번호 틀렸을 때 알림창 생성 함수
local function createErrorNotification(parentFrame)
    local errorNotification = Instance.new("Frame")
    errorNotification.Size = UDim2.new(0, 0, 0, 0)
    errorNotification.Position = UDim2.new(0.5, 0, 0, -50) -- 상단에 위치
    errorNotification.AnchorPoint = Vector2.new(0.5, 0)
    errorNotification.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    errorNotification.BorderSizePixel = 0
    setRoundedCorners(errorNotification, 20) -- 모서리 곡선 강도를 조정합니다.
    errorNotification.Parent = parentFrame

    local contentLabel = Instance.new("TextLabel")
    contentLabel.Size = UDim2.new(1, 0, 1, 0)
    contentLabel.BackgroundTransparency = 1
    contentLabel.Text = "비밀번호를 틀렸습니다."
    contentLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    contentLabel.Font = Enum.Font.Gotham
    contentLabel.TextSize = 18
    contentLabel.TextStrokeTransparency = 0.8
    contentLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    contentLabel.Parent = errorNotification

    -- 애니메이션으로 오류 메시지 확장
    local expandTween = TweenService:Create(errorNotification, TweenInfo.new(0.5, Enum.EasingStyle.Bounce, Enum.EasingDirection.Out), {Size = UDim2.new(0.8, 0, 0.1, 0)}) -- 크기 조정
    expandTween:Play()

    -- 1.5초 후 자동으로 알림창 제거
    delay(1.5, function()
        local shrinkTween = TweenService:Create(errorNotification, TweenInfo.new(0.5, Enum.EasingStyle.Sine, Enum.EasingDirection.In), {Size = UDim2.new(0, 0, 0, 0)}) -- 크기 조정
        shrinkTween:Play()
        shrinkTween.Completed:Connect(function() errorNotification:Destroy() end)
    end)
end

-- 비밀번호 입력과 알림 창 설정
local function setup()
    local passwordScreen, passwordBox, submitButton, mainFrame = createPasswordScreen()

    -- 비밀번호가 맞는지 확인
    local correctPassword = "test123"  -- 임시 비밀번호

    submitButton.MouseButton1Click:Connect(function()
        if passwordBox.Text == correctPassword then
            passwordScreen:Destroy()
        else
            passwordBox.Text = ""
            createErrorNotification(mainFrame)
        end
    end)
end

setup()